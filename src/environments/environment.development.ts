// Entorno de DESARROLLO. Se activa con `ng build/serve --configuration development`
// gracias al fileReplacements de angular.json.
export const environment = {
  production: false,
  API_URL: 'http://localhost:8080',
};
